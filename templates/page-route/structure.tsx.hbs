      S.listItem()
        .title("{{titleCase routePrefix}}")
        .icon(() => <>📄</>)
        .child(S.documentTypeList("{{camelCase documentType}}").title("{{titleCase routePrefix}}")),

      // PLOP: Add Structure
